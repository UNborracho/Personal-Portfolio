node_modules/
dist/
build/
.cache/
.env*
.vite/
vite.config.*.timestamp-*
*.tsbuildinfo
*-debug.log*
logs/
*.log
.claude-replay.*
.tmp-*

# photo originals (1.1GB) — webp derivatives in public/photos/ ARE tracked
photo/

# macOS
.DS_Store

# figma make internals — only make/site.json stays tracked (vite.config imports it)
.figma/*
!.figma/make/
.figma/make/*
!.figma/make/site.json

# internal working docs (not part of the shipped site)
site-info.md
src/imports/

# reference-site decompilation archive — local only (public repo: no third-party
# decompiled code tracked; the spec moved to docs/animation-spec.md)
.scratch/

# legacy ffmpeg-era pipeline (superseded by scripts/photos.mjs webp pipeline)
scripts/compress-photos.sh
scripts/photo-manifest.tsv

# internal working docs — local only (public repo)
RULES.md
product.md
CLAUDE.md
